"""Re-crawl + canonical-hash diff -> append QueueEvent to review_queue.jsonl (SCOPE §6, A09). Stub — G7.
"""
# TODO(G7): re-fetch+render+extract; canonical_hash vs stored; changed/unreachable -> QueueEvent
# (append-only + portalocker + dedupe).
def diff_recrawl():
    raise NotImplementedError("G7")

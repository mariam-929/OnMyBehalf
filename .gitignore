# ---- secrets (NEVER commit) ----
.env
.env.*
!.env.example
*.key
*secret*
*apikey*
*api_key*
# any loose key text files, wherever they land
groq*.txt
key*.txt

# ---- data / models / caches (churn OneDrive; regenerated) ----
data/chroma/
data/corpus/
data/corpus_unmatched.json
data/contacts.json
data/catalog.json
data/review_queue.jsonl
logs/
*.log
# report/ and docs/ are not part of the shipped repo; some scripts (check_g6,
# report_to_docx) still write there, so keep the output out of git.
report/
docs/

# ---- python ----
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# ---- playwright / node ----
ms-playwright/
node_modules/

# ---- os / editor ----
.DS_Store
Thumbs.db
.vscode/
.idea/
